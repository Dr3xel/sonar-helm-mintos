apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql.name" . }}
  labels:
    {{- include "postgresql.labels" . | nindent 4 }}
    app: postgresql
spec:
  selector:
    app: postgresql
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres